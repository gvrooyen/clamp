(executable
 (name kb)
 (public_name kb)
 (libraries clamp cmdliner)
 (link_flags
  (:standard (:include linker_flags.sexp))))

(rule
 (target linker_flags.sexp)
 (action (run sh %{dep:linker-flags} %{system})))
